#pragma once

#include <string>

enum class BLOCK_TYPE{
	AIR,
	WALL,
	BUTTON,
	ERROR
};

enum class BUTTON_TYPE{
	NOTHING,
	ESCAPE_RESUME,
	ESCAPE_RESTART,
	ESCAPE_SETTINGS,
	ESCAPE_MAIN_MENU,
	ESCAPE_PAUSED,
	SETTINGS_EDITOR_TOGGLE
};

enum class GAME_STATE{
	PLAYING,
	ESCAPE,
	SETTINGS,
	MAIN_MENU
};

enum class CMAERA_TYPE{
	BOTH,
	PLAYER1,
	PLAYER2
};


BLOCK_TYPE STRING_TO_BLOCK_TYPE(std::string the_string);