#pragma once

enum class BLOCK_TYPE{
	AIR,
	WALL,
	BUTTON,
	ERROR
};

enum class BUTTON_TYPE{
	NOTHING,
	ESCAPE_RESUME,
	ESCAPE_RESTART,
	ESCAPE_SETTINGS,
	ESCAPE_MAIN_MENU,
	ESCAPE_PAUSED
};

enum class GAME_STATE{
	PLAYING,
	ESCAPE,
	SETTINGS,
	MAIN_MENU
};